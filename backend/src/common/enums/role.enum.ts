// Defines the roles allowed in the school admission system.
// This is used for authorization and role-based access control (RBAC).
export enum Role {
  PARENT = 'parent',
  ADMISSION_TEAM = 'admission_team',
}
